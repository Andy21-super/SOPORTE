# Instalacion de Node.js

Este equipo no tiene `node` ni `npm` disponibles en la consola.

Instala Node.js 22 LTS desde:

https://nodejs.org/

Despues cierra y vuelve a abrir PowerShell o CMD, y valida:

```bash
node -v
npm -v
```

Luego ejecuta `INICIAR_LOCAL.bat`.
